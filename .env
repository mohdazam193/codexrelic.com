# FastAPI & MongoDB Configuration
# Replace the MONGO_URI with your MongoDB Atlas connection string when ready:
# MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/codexrelic?retryWrites=true&w=majority
MONGO_URI=mongodb://localhost:27017

# Default Seeding Credentials (Auto-created on first boot if DB is empty)
ADMIN_USER=admin
ADMIN_PASS=codexrelic
ADMIN_CODE=123456
